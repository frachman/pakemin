# Project Overrides

Documentation-only tasks in this sample should not introduce application source code.

When tenant isolation is relevant, security rules take priority over convenience or speed.

Adapters must remain thin. If an adapter grows beyond entry-point guidance, move content into `.ai`.

