# Repository Map

This sample assumes the following application layout:

```text
.
├── apps
│   ├── web
│   ├── api
│   └── worker
├── packages
│   ├── config
│   ├── database
│   └── ui
└── .ai
```

The layout is not included as implementation code in this example.

