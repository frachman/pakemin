# Technology Stack

This sample assumes a conventional web SaaS stack:

- TypeScript for application code.
- React for the web interface.
- Node.js for API and worker processes.
- PostgreSQL for relational data.
- Redis for short-lived queues and cache data.

The stack is illustrative and does not define a required AES technology choice.

